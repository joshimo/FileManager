package sw_shell;

/** Viewing sw_shell Class */
public class TestShell {

    /*Vector<FileUnit> fileList = new Vector<>();
    Vector<File> filesToCopyList;
    FileController controller = FileController.getInstance();

    File source = new File("D:\\!Программирование\\1");
    File destination = new File("D:\\!Программирование\\!empty\\Белый Яр.rar");
    File sourceFolder = new File("D:\\Будапешт");
    File destinationFolder = new File("D:\\!Программирование\\!empty");
    File newDir = new File("D:\\!Программирование\\!empty\\!inside\\1\\2");
    File deleatedFile = new File("D:\\!Программирование\\!empty\\Белый Яр.rar");
    File deleatedDirectory = new File("D:\\!Программирование\\!empty\\Навител");

    TestShell() {
        try {
            controller.showMonitor();
            fileList = controller.getFileList(new File("D:\\!Программирование"));
            //controller.CopyFile(source, destination);
            //controller.createNewDirectory(newDir);
            //controller.DeleteFile(deleatedFile);
            //controller.DeleteDirectory(deleatedDirectory);
            //controller.CopyDirectory(sourceFolder, destinationFolder);
            //filesToCopyList = controller.getSourceFileList();
            controller.hideMonitor();
        }
        catch (IOException ioe) {
            System.out.println("IO Exception: " + ioe.toString());
        }
        for (File f : fileList) System.out.println(f.getAbsolutePath());
        String A = "abcDEF";
        String B = "abc";
        String C = "ABC";
        System.out.println(C.concat(A.replaceFirst(B, "")));
        System.out.println(deleatedFile.getParent());
    }

    public static void main(String... args) {
        new TestShell();
    }*/
}
